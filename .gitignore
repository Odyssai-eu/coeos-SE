# Never commit the runtime config — it can hold API keys.
coeos-config.json
coeos-config.json.tmp
data/
.env

__pycache__/
*.pyc
*.egg-info/
dist/
build/
.venv/
.pytest_cache/
.DS_Store

# graphify local knowledge graph (per-repo, ~14 MB) — lives in the stack graph
graphify-out/

# Internal working docs — never publish (kept local only)
CLAUDE.md
plan.md
docs/VELOCITY.md
doc/sessions/
